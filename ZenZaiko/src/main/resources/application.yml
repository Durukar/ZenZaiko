spring:
  datasource:
    url: jdbc:h2:mem:zenzaiko
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    spring:
        jpa:
          database-platform: org.hibernate.dialect.H2Dialect
          defer-datasource-initialization: false
  h2:
    console:
      enabled: true

